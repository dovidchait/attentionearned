node_modules/
dist/
.env
.env.local
.env.production

# Real donor data — NEVER commit these
*.csv
*.xlsx
*.xls
!test/fixtures/*.xlsx
!test/fixtures/*.csv

# Only synthetic fixture files may be committed — enforce by directory
# Real data would typically live in data/ or uploads/ which are excluded:
data/
uploads/
exports/

# Build artifacts
*.js.map
*.d.ts.map

# OS
.DS_Store
